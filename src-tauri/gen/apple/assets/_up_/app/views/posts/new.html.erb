<div class="container">
  <div class="page-header">
    <h1>📝 Create New Post</h1>
    <p>Share text, images, or videos with end-to-end encryption</p>
  </div>

  <div class="form-container">
    <div class="form-section">
      <%= form_with model: @post, multipart: true, class: "post-form", 
                    data: { controller: "post-creation" } do |form| %>
        <% if @post.errors.any? %>
          <div class="error-messages">
            <h3><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h3>
            <ul>
              <% @post.errors.full_messages.each do |message| %>
                <li><%= message %></li>
              <% end %>
            </ul>
          </div>
        <% end %>

        <div class="form-group">
          <%= form.label :content, "What's on your mind?", class: "form-label" %>
          <%= form.text_area :content, class: "form-control post-textarea", 
                              placeholder: "Write your thoughts, share updates, or describe what you're posting...",
                              rows: 6 %>
        </div>

        <div class="form-group">
          <label class="form-label">Media Attachments</label>
          <div class="file-upload-area" 
               data-post-creation-target="uploadArea"
               data-action="drop->post-creation#handleDrop">
            <div class="file-upload-prompt">
              <div class="upload-icon">📎</div>
              <p><strong>Drag & drop files here</strong> or click to browse</p>
              <p class="upload-hint">Supports images, videos, and other files</p>
              <input type="file" 
                     name="attachments[]" 
                     data-post-creation-target="fileInput"
                     data-action="change->post-creation#handleFileSelect"
                     class="file-input" 
                     multiple 
                     accept="image/*,video/*,audio/*,.pdf,.doc,.docx,.txt">
            </div>
            
            <div class="file-previews" data-post-creation-target="previews"></div>
          </div>
        </div>

        <div class="encryption-info">
          <h3>🔐 Privacy & Security</h3>
          <ul>
            <li><strong>End-to-End Encrypted:</strong> Your content is encrypted before leaving your device</li>
            <li><strong>Secure Storage:</strong> Files are stored encrypted on the network</li>
            <li><strong>Your Keys:</strong> Only you (and those you share with) can decrypt this content</li>
          </ul>
        </div>

        <div class="form-actions">
          <%= form.submit "📤 Post Securely", class: "btn btn-primary btn-large", id: "post-submit-btn" %>
          <%= link_to "Cancel", root_path, class: "btn btn-secondary" %>
        </div>
      <% end %>
    </div>

    <div class="info-section">
      <h3>📚 How it works</h3>
      <ol>
        <li><strong>Write & Upload:</strong> Add your text and any media files</li>
        <li><strong>Encrypt:</strong> Everything is encrypted client-side with your keys</li>
        <li><strong>Post:</strong> Encrypted content is shared on the network</li>
        <li><strong>Decrypt:</strong> Only you and authorized users can view the content</li>
      </ol>

      <div class="supported-formats">
        <h4>Supported Media</h4>
        <div class="format-list">
          <span class="format-tag">🖼️ Images (JPG, PNG, GIF, WebP)</span>
          <span class="format-tag">🎥 Videos (MP4, WebM, MOV)</span>
          <span class="format-tag">🎵 Audio (MP3, WAV, OGG)</span>
          <span class="format-tag">📄 Documents (PDF, DOC, TXT)</span>
        </div>
      </div>
    </div>
  </div>
</div>

<style>
.post-form {
  max-width: 800px;
}

.post-textarea {
  min-height: 120px;
  resize: vertical;
  font-family: inherit;
  line-height: 1.5;
}

.file-upload-area {
  border: 2px dashed #ddd;
  border-radius: 12px;
  padding: 20px;
  text-align: center;
  background: #fafafa;
  transition: all 0.3s ease;
  position: relative;
}

.file-upload-area:hover,
.file-upload-area.drag-over {
  border-color: #667eea;
  background: #f0f4ff;
}

.file-upload-prompt {
  padding: 20px;
}

.upload-icon {
  font-size: 3em;
  margin-bottom: 10px;
  opacity: 0.6;
}

.file-upload-prompt p {
  margin: 8px 0;
  color: #666;
}

.upload-hint {
  font-size: 0.9em;
  opacity: 0.8;
}

.file-input {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  opacity: 0;
  cursor: pointer;
}

.file-previews {
  margin-top: 20px;
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(150px, 1fr));
  gap: 15px;
}

.file-preview {
  position: relative;
  border: 1px solid #ddd;
  border-radius: 8px;
  padding: 10px;
  background: white;
  text-align: center;
}

.file-preview img,
.file-preview video {
  max-width: 100%;
  height: 120px;
  object-fit: cover;
  border-radius: 4px;
}

.file-preview-info {
  margin-top: 8px;
  font-size: 0.8em;
  color: #666;
}

.file-preview-name {
  font-weight: 500;
  margin-bottom: 4px;
  word-break: break-word;
}

.file-preview-size {
  opacity: 0.8;
}

.remove-file {
  position: absolute;
  top: -8px;
  right: -8px;
  background: #ff4444;
  color: white;
  border: none;
  border-radius: 50%;
  width: 24px;
  height: 24px;
  cursor: pointer;
  font-size: 0.8em;
  display: flex;
  align-items: center;
  justify-content: center;
}

.remove-file:hover {
  background: #cc0000;
}

.encryption-info {
  background: rgba(255, 255, 255, 0.9);
  border: 1px solid #e0e0e0;
  border-radius: 8px;
  padding: 20px;
  margin: 20px 0;
}

.encryption-info h3 {
  margin-top: 0;
  color: #2d3748;
}

.encryption-info ul {
  margin: 0;
  padding-left: 20px;
}

.encryption-info li {
  margin: 8px 0;
  color: #4a5568;
}

.supported-formats {
  background: #f8f9fa;
  border-radius: 8px;
  padding: 15px;
  margin-top: 20px;
}

.format-list {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
  margin-top: 10px;
}

.format-tag {
  background: #e9ecef;
  padding: 4px 8px;
  border-radius: 4px;
  font-size: 0.85em;
  white-space: nowrap;
}
</style>

