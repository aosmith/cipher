<div class="container">
  <div class="welcome-hero">
    <h2>📝 Create New Post</h2>
    <p>Share text, images, or videos with end-to-end encryption</p>
  </div>

  <div class="form-container">
    <div class="form-section">
      <%= form_with model: @post, multipart: true, class: "post-form", local: true do |form| %>
        <% if @post.errors.any? %>
          <div class="error-messages">
            <h3><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h3>
            <ul>
              <% @post.errors.full_messages.each do |message| %>
                <li><%= message %></li>
              <% end %>
            </ul>
          </div>
        <% end %>

        <div class="form-group">
          <%= form.label :content, "What's on your mind?", class: "form-label" %>
          <%= form.text_area :content, class: "form-control post-textarea", 
                              placeholder: "Write your thoughts, share updates, or describe what you're posting...",
                              rows: 6 %>
        </div>

        <div class="form-group">
          <label class="form-label" for="post-attachments">Media Attachments</label>
          <input type="file" name="attachments[]" id="post-attachments" class="form-control" multiple accept="image/*,video/*,audio/*,.pdf,.doc,.docx,.txt">
          <p class="form-help">Attach optional media files. Decrypt them on your server after download.</p>
        </div>

        <div class="encryption-info">
          <h3>🔐 Privacy & Security</h3>
          <ul>
            <li><strong>End-to-End Encrypted:</strong> Your content is encrypted before leaving your device</li>
            <li><strong>Secure Storage:</strong> Files are stored encrypted on the network</li>
            <li><strong>Your Keys:</strong> Only you (and those you share with) can decrypt this content</li>
          </ul>
        </div>

        <div class="form-actions">
          <%= form.submit "📤 Post Securely", class: "btn btn-primary btn-large", id: "post-submit-btn" %>
          <%= link_to "Cancel", root_path, class: "btn btn-secondary" %>
        </div>
      <% end %>
    </div>

    <div class="info-section">
      <h3>📚 How it works</h3>
      <ol>
        <li><strong>Write & Upload:</strong> Add your text and any media files</li>
        <li><strong>Encrypt:</strong> Everything is encrypted client-side with your keys</li>
        <li><strong>Post:</strong> Encrypted content is shared on the network</li>
        <li><strong>Decrypt:</strong> Only you and authorized users can view the content</li>
      </ol>

      <div class="supported-formats">
        <h4>Supported Media</h4>
        <div class="format-list">
          <span class="format-tag">🖼️ Images (JPG, PNG, GIF, WebP)</span>
          <span class="format-tag">🎥 Videos (MP4, WebM, MOV)</span>
          <span class="format-tag">🎵 Audio (MP3, WAV, OGG)</span>
          <span class="format-tag">📄 Documents (PDF, DOC, TXT)</span>
        </div>
      </div>
    </div>
  </div>
</div>

<style>
.post-form {
  max-width: 800px;
}

.post-textarea {
  min-height: 120px;
  resize: vertical;
  font-family: inherit;
  line-height: 1.5;
}

.form-help {
  font-size: 0.9em;
  color: #666;
  margin-top: 6px;
}

.encryption-info {
  background: rgba(255, 255, 255, 0.9);
  border: 1px solid #e0e0e0;
  border-radius: 8px;
  padding: 20px;
  margin: 20px 0;
}

.supported-formats {
  background: #f8f9fa;
  border-radius: 8px;
  padding: 15px;
  margin-top: 20px;
}

.format-list {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
  margin-top: 10px;
}

.format-tag {
  background: #e9ecef;
  padding: 4px 8px;
  border-radius: 4px;
  font-size: 0.85em;
  white-space: nowrap;
}
</style>
